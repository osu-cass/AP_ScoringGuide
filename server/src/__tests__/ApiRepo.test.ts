import * as Mocks from "./Mocks";
import {ApiRepo} from "../ApiRepo";
describe("ApiRepo.getAssociatedItems", () => {
    it("returns associated items", () => {
        let repo = new ApiRepo(Mocks.sgVMGetter, )
    });
});